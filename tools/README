Those are support tools for the project.

./plugins-page-gem.py
   generates a github wiki compatible page with all the plugin
   found in github.
   This... takes a while with the API rate limit.
   It will write out a Home.md.
   It will also update a blacklist of false positive on the initial research to optimize subsequent ones.

